import React, {useState} from 'react';
export const ExtractionView: React.FC = () => {
  const [filter,setFilter]=useState('high');
  return <div><h2>EXTRACTION - Extraction - names, dates, places, relat</h2><p>names</p></div>
};
export default ExtractionView;
